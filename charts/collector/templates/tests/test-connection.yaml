apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-gh-collector.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-gh-collector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-gh-collector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
